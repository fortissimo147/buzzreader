PORT=3001

# Twitter API v2 Bearer Token (https://developer.twitter.com)
# Leave empty to run in DEMO mode (no real API calls)
TWITTER_BEARER_TOKEN=

# DEMO_MODE=true uses generated mock data (default when no bearer token)
DEMO_MODE=true

# Polling interval in minutes
POLL_INTERVAL_MINUTES=15
